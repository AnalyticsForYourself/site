---
import Layout from "../../../layouts/Layout.astro";
import Dashboard from "../../../components/Dashboard.astro";
const { user } = Astro.params;
if (!user) return Astro.redirect('/');
export const prerender = false;
---
<Layout>
    <Dashboard user={parseInt(user, 10)} />
</Layout>