<section>
    <h1 class="display-3">The simple, easy to use, analytics solution for Fan Fic Authors on AO3</h1>
    <p class="h5">Understand how your works are received without relying on invasive and tedious tools or reader interaction.</p>
    <form class="row justify-content-center">
        <div class="col-lg-8">
            <div class="input-group">
                <div class="form-floating">
                    <input class="form-control" name="username" id="username" placeholder=" " />
                    <label for="username">Sign up now with your AO3 username</label>
                </div>
                <input type="submit" class="btn btn-primary" value="Sign Up" />
            </div>
        </div>
    </form>
</section>
<style>
    h1 {
        text-wrap: balance;
    }
    p {
        color: var(--text-600);
        text-wrap: balance;
        margin: 0;
        margin-top: 1em;
    }
    section {
        min-height: 50vh;
        display: grid;
        place-content: center;
        text-align: center;
        padding-inline: 10vw;
    }
    form {
        padding: 1rem;
        border-radius: calc(Infinity * 1px);
        color: var(--text-600);
        align-items: center;
    }
    label {
        display: block;
    }
    .form-control,
    .form-group,
    .input-group:not(.has-validation)>.dropdown-toggle:nth-last-child(n+3), .input-group:not(.has-validation)>.form-floating:not(:last-child)>.form-control, .input-group:not(.has-validation)>.form-floating:not(:last-child)>.form-select, .input-group:not(.has-validation)>:not(:last-child):not(.dropdown-toggle):not(.dropdown-menu):not(.form-floating) {
        border-radius: calc(Infinity * 1px);
    }
    .form-floating > label {
        padding-left: 2ch;
    }
    .form-floating>.form-control {
        padding-left: 2ch;
    }
    .form-floating>.form-control-plaintext~label, .form-floating>.form-control:focus~label, .form-floating>.form-control:not(:placeholder-shown)~label, .form-floating>.form-select~label {
        /*transform: scale(.85) translateY(-.5rem) translateX(.65rem);*/
    }
    .form-floating>.form-control-plaintext:focus, .form-floating>.form-control-plaintext:not(:placeholder-shown), .form-floating>.form-control:focus, .form-floating>.form-control:not(:placeholder-shown) {
        padding-bottom: .75rem;
    }
    .form-floating {
        z-index: 1;
    }
    .btn {
        border-top-right-radius: calc(Infinity * 1px);
        border-bottom-right-radius: calc(Infinity * 1px);
        padding-left: 3rem;
        margin-left: -2.5rem !important;
        position: relative;
        z-index: 0;
    }

</style>